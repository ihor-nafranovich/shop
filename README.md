# Laravel E-Shop - Интернет-магазин на Laravel

<p align="center">
  <img src="https://raw.githubusercontent.com/laravel/art/master/logo-lockup/5%20SVG/2%20CMYK/1%20Full%20Color/laravel-logolockup-cmyk-red.svg" width="200" alt="Laravel Logo">
  <br>
  <strong>Современный интернет-магазин на Laravel с Livewire</strong>
</p>

## 📋 Описание проекта

Laravel E-Shop — это полнофункциональный интернет-магазин, построенный на Laravel с использованием Livewire для создания динамических пользовательских интерфейсов. Проект включает в себя административную панель для управления товарами, категориями, заказами и пользователями.

## ✨ Основные возможности

### 🛍️ Пользовательская часть
- **Каталог товаров** с категориями и фильтрами
- **Корзина покупок** с динамическим обновлением
- **Система поиска** товаров
- **Личный кабинет** пользователя
- **Оформление заказов** с уведомлениями
- **Адаптивный дизайн** на Bootstrap 5

### 🔧 Административная панель
- **Управление товарами** (создание, редактирование, удаление)
- **Управление категориями** товаров
- **Система фильтров** для товаров
- **Управление заказами** и их статусами
- **Управление пользователями**
- **Файловый менеджер** для загрузки изображений
- **Статистика и аналитика**

## 🛠️ Технологический стек

### Backend
- **Laravel 12** — PHP фреймворк
- **Livewire 3.6** — для динамических компонентов
- **MySQL** — база данных
- **Eloquent ORM** — работа с БД
- **Mail** — система уведомлений
- **Eloquent Sluggable** — для генерации slug-ов

### Frontend
- **Bootstrap 5** — CSS фреймворк
- **Livewire** — динамические компоненты
- **OwlCarousel** — слайдеры
- **FontAwesome** — иконки
- **Chart.js** — графики и диаграммы
- **Vite** — сборка фронтенда
- **Tailwind CSS** — утилитарный CSS фреймворк

## 📁 Структура проекта

```
laravel-eshop.loc/
├── app/
│   ├── Helpers/          # Вспомогательные классы
│   ├── Http/
│   │   ├── Controllers/  # Контроллеры
│   │   └── Livewire/     # Livewire компоненты
│   ├── Mail/             # Классы для отправки писем
│   ├── Models/           # Eloquent модели
│   └── Providers/        # Сервис-провайдеры
├── resources/views/      # Blade шаблоны
├── public/              # Публичная директория
├── database/            # Миграции и сидеры
└── routes/              # Маршруты
```

## 🚀 Установка и настройка

### Требования
- PHP >= 8.2
- Composer
- MySQL >= 5.7
- Node.js & NPM

### Шаги установки

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd laravel-eshop.loc
   ```

2. **Установка PHP зависимостей**
   ```bash
   composer install
   ```
   ```bash
   composer require cviebrock/eloquent-sluggable
   ```

3. **Установка Node.js зависимостей**
   ```bash
   npm install
   ```

4. **Настройка окружения**
   ```bash
   cp .env.example .env
   php artisan key:generate
   ```

5. **Настройка базы данных**
   - Создайте базу данных MySQL
   - Обновите настройки в `.env`
   - Импортируйте дамп БД: `laravel_eshop.sql`

6. **Запуск миграций и сидеров**
   ```bash
   php artisan migrate
   php artisan db:seed
   ```

7. **Сборка фронтенда**
   ```bash
   npm run build
   ```

8. **Запуск сервера**
   ```bash
   php artisan serve
   ```

## 📊 Основные сущности

### Модели данных
- **User** — пользователи системы
- **Category** — категории товаров
- **Product** — товары
- **Order** — заказы
- **Filter/FilterGroup** — фильтры товаров
- **Cart** — корзина покупок
- **ContactLead** — контактные формы
- **Media** — медиа файлы

### Livewire компоненты
- **Admin/** — административные компоненты
- **Cart/** — компоненты корзины
- **Product/** — компоненты товаров
- **User/** — компоненты пользователей
- **Search/** — компоненты поиска

## 🔐 Безопасность

- Аутентификация и авторизация пользователей
- Middleware для защиты административной панели
- Валидация данных на стороне сервера
- Защита от CSRF атак
- Безопасная загрузка файлов

## 📧 Уведомления

Система автоматических уведомлений:
- **OrderClient** — уведомления клиентам о заказах
- **OrderManager** — уведомления менеджерам о новых заказах

## 🧪 Тестирование

Проект включает тесты:
- **Feature тесты** — тестирование функциональности
- **Unit тесты** — модульное тестирование
- **Laravel Pint** — форматирование кода
- **PHPUnit** — фреймворк для тестирования

Запуск тестов:
```bash
php artisan test
```

Запуск форматирования кода:
```bash
composer pint
```

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

---

## 🧹 Очистка проекта

В рамках технического обслуживания проекта была проведена очистка от временных и неиспользуемых файлов:

### Удаленные файлы и папки

✅ **Временные файлы загрузки** (30 файлов)
- Путь: `public/uploads/livewire-tmp/`
- Тип: Временные файлы загрузки Livewire
- Действие: Успешно удалены все файлы
- Команда: `find public/uploads/livewire-tmp -type f -delete`

### Потенциально лишние элементы (требуют решения)

📂 **Папка конфигурации IDE** `.idea/`
- Содержимое: 10 файлов конфигурации PHPStorm
- Статус: Уже исключена из репозитория через `.gitignore`
- Рекомендация: Удалить физически, если проект не использует PHPStorm
- Команда для удаления: `rm -rf .idea/`

📄 **Дублирующиеся файлы дипломной работы**
- Файлы: `1.docx` и `1.md`
- Содержимое: Один и тот же контент в разных форматах
- Рекомендация: Оставить только `1.md` (Markdown формат)
- Команда для удаления: `rm 1.docx`

### Рекомендации по поддержанию чистоты проекта

1. **Регулярная очистка временных файлов**:
   ```bash
   # Очистка временных файлов загрузки Livewire
   find public/uploads/livewire-tmp -type f -delete

   # Очистка кэша Laravel
   php artisan cache:clear
   php artisan view:clear
   php artisan route:clear
   php artisan config:clear
   ```

2. **Удаление неиспользуемых зависимостей**:
   ```bash
   # Для Composer
   composer remove <неиспользуемый-пакет>

   # Для NPM
   npm uninstall <неиспользуемый-пакет>
   ```

3. **Проверка больших файлов**:
   ```bash
   # Найти большие файлы (>10MB)
   find . -type f -size +10M

   # Найти неотслеживаемые файлы
   git status --porcelain
   ```

4. **Оптимизация автозагрузки Composer**:
   ```bash
   composer dump-autoload -o
   ```

---

## 📚 Дополнительные ресурсы

- [Документация Laravel](https://laravel.com/docs)
- [Документация Livewire](https://laravel-livewire.com/docs)
- [Bootstrap 5 документация](https://getbootstrap.com/docs/5.0/)
- [Tailwind CSS документация](https://tailwindcss.com/docs)
- [Eloquent Sluggable документация](https://github.com/cviebrock/eloquent-sluggable)

---

<p align="center">
<a href="https://github.com/laravel/framework/actions"><img src="https://github.com/laravel/framework/workflows/tests/badge.svg" alt="Build Status"></a>
<a href="https://packagist.org/packages/laravel/framework"><img src="https://img.shields.io/packagist/dt/laravel/framework" alt="Total Downloads"></a>
<a href="https://packagist.org/packages/laravel/framework"><img src="https://img.shields.io/packagist/v/laravel/framework" alt="Latest Stable Version"></a>
<a href="https://packagist.org/packages/laravel/framework"><img src="https://img.shields.io/packagist/l/laravel/framework" alt="License"></a>
</p>

## About Laravel

Laravel is a web application framework with expressive, elegant syntax. We believe development must be an enjoyable and creative experience to be truly fulfilling. Laravel takes the pain out of development by easing common tasks used in many web projects, such as:

- [Simple, fast routing engine](https://laravel.com/docs/routing).
- [Powerful dependency injection container](https://laravel.com/docs/container).
- Multiple back-ends for [session](https://laravel.com/docs/session) and [cache](https://laravel.com/docs/cache) storage.
- Expressive, intuitive [database ORM](https://laravel.com/docs/eloquent).
- Database agnostic [schema migrations](https://laravel.com/docs/migrations).
- [Robust background job processing](https://laravel.com/docs/queues).
- [Real-time event broadcasting](https://laravel.com/docs/broadcasting).

Laravel is accessible, powerful, and provides tools required for large, robust applications.

## Learning Laravel

Laravel has the most extensive and thorough [documentation](https://laravel.com/docs) and video tutorial library of all modern web application frameworks, making it a breeze to get started with the framework.

You may also try the [Laravel Bootcamp](https://bootcamp.laravel.com), where you will be guided through building a modern Laravel application from scratch.

If you don't feel like reading, [Laracasts](https://laracasts.com) can help. Laracasts contains thousands of video tutorials on a range of topics including Laravel, modern PHP, unit testing, and JavaScript. Boost your skills by digging into our comprehensive video library.

## Laravel Sponsors

We would like to extend our thanks to the following sponsors for funding Laravel development. If you are interested in becoming a sponsor, please visit the [Laravel Partners program](https://partners.laravel.com).

### Premium Partners

- **[Vehikl](https://vehikl.com/)**
- **[Tighten Co.](https://tighten.co)**
- **[WebReinvent](https://webreinvent.com/)**
- **[Kirschbaum Development Group](https://kirschbaumdevelopment.com)**
- **[64 Robots](https://64robots.com)**
- **[Curotec](https://www.curotec.com/services/technologies/laravel/)**
- **[Cyber-Duck](https://cyber-duck.co.uk)**
- **[DevSquad](https://devsquad.com/hire-laravel-developers)**
- **[Jump24](https://jump24.co.uk)**
- **[Redberry](https://redberry.international/laravel/)**
- **[Active Logic](https://activelogic.com)**
- **[byte5](https://byte5.de)**
- **[OP.GG](https://op.gg)**

## Contributing

Thank you for considering contributing to the Laravel framework! The contribution guide can be found in the [Laravel documentation](https://laravel.com/docs/contributions).

## Code of Conduct

In order to ensure that the Laravel community is welcoming to all, please review and abide by the [Code of Conduct](https://laravel.com/docs/contributions#code-of-conduct).

## Security Vulnerabilities

If you discover a security vulnerability within Laravel, please send an e-mail to Taylor Otwell via [taylor@laravel.com](mailto:taylor@laravel.com). All security vulnerabilities will be promptly addressed.

## License

The Laravel framework is open-sourced software licensed under the [MIT license](https://opensource.org/licenses/MIT).

## 🧭 Взаимодействия фронтенда (main.js, main.css)

Ниже описаны все ключевые взаимодействия и зависимости, реализованные в публичных фронтенд-активах проекта.

### Файлы и назначение
- `public/assets/js/main.js`: клиентская логика для пользовательской части (хедер при скролле, «наверх», слайдеры, Toastr, интеграция с Livewire).
- `public/assets/css/main.css`: базовые стили, палитра, компоненты интерфейса, карточки товаров, поиск, корзина, пагинация, Toastr-скины, адаптив.
- `public/assets/admin/main.js`: глобальная конфигурация Toastr для админ-панели.
- `public/assets/admin/main.css`: служебные стили админки (Toastr-скины, индикаторы, переключатели, уплотнение меню).

### DOM-хуки (элементы, от которых зависит поведение)
- `#header-nav` — контейнер шапки (переключается класс `headernav-scroll` при скролле > 135px).
- `#top` — кнопка «наверх» (показывается при скролле > 300px; клик плавно прокручивает страницу к началу).
- `.owl-carousel-full` — карусели товаров/контента (инициализируются/переинициализируются OwlCarousel).
- Поиск: `.search-form`, `.search-results`, `.search-empty` — стили и позиционирование выпадающих результатов.
- Оффканвас-корзина: таблица `.offcanvasCart-table` — размеры ячеек и изображений.

### main.js — поведение и события
- Скролл страницы:
  - Слушатель `window.scroll`: переключает `headernav-scroll` у `#header-nav` при `window.scrollY > 135`.
  - При jQuery-скролле окна показывается/скрывается `#top` (порог 300px).
- Кнопка «наверх» (`#top`): клик плавно прокручивает `html, body` к началу за 500ms.
- Livewire интеграция:
  - На событие `livewire:navigated` выполняется пересборка слайдеров: сначала `$('.owl-carousel-full').owlCarousel('destroy')`, затем повторная инициализация с `margin: 20` и responsive-сеткой (1/2/3/4 айтема на брейкпоинтах 0/500/700/1000).
  - На том же событии повторно вешается обработчик клика `#top` (для страниц, пришедших через навигацию Livewire).
- Toastr уведомления (глобально):
  - Включена полоса прогресса, позиция `toast-bottom-right`, `timeOut: 4000`, методы `show/hide: slideDown/slideUp` и пр. (см. файл).
- Требуемые зависимости (должны быть загружены до использования):
  - jQuery, OwlCarousel, Toastr, Livewire (для события `livewire:navigated`).

### main.css — ключевые стили и компоненты
- Палитра и базовые правила: CSS-переменные (`--bg-color`, `--accent-color` и др.), типографика, контроль фокуса у форм, скругления, плейсхолдеры.
- Toastr-скины: отдельные стили для `.toast-success` и `.toast-error` с левым цветным бордером, иконками (Font Awesome), списками без маркеров; непрозрачные тосты (без теней/фоновых картинок).
- Хедер/навигация: бордер и тень нижней части хедера, цвета ссылок, hover/active состояния, dropdown-меню, компактные паддинги при `headernav-scroll`.
- Поиск: абсолютное позиционирование выпадающих результатов `.search-results`, интерактивные строки, крестик очистки `.search-empty`.
- Карточки товаров: бордер/тени, бейджи офферов (`.offer-hit`, `.offer-new`), ограничение строк названия/описания через `-webkit-line-clamp`, цены (включая `small` со зачёркиванием), рейтинг.
- Разделы: преимущества `.advantages`, белые секции (в т.ч. `.about-us`), футер с ссылками/иконками.
- Кнопка «наверх» `#top`: фиксированная позиция, размеры, полупрозрачность по умолчанию и hover-усиление.
- Категории/фильтры/пагинация: хлебные крошки, сайдбар, чекбоксы, выбранные фильтры, состояния пагинации.
- Страница товара: цена, ширина input-group, список характеристик с маркерами от Font Awesome, табы.
- Корзина/checkout: размеры миниатюр, отступы, поле количества, стили купона, суммарные блоки, `#checkoutForm .ajax-loader` с плавной анимацией `fade`.
- Медиа-запросы: корректировки паддингов nav, управление видимостью блока фильтров на ≥768px, сужение табов на ≤500px.

### Админ-активы
- `public/assets/admin/main.js`: только конфигурация Toastr (та же, что и на витрине).
- `public/assets/admin/main.css`: Toastr-стили, индикатор `.update-loading`, уведомление `.media-copied`, тумблер `.switch` (в т.ч. `.slider.round`), уменьшенные отступы у ссылок сайдбара и подсветка активного.

### Отличия демо-шаблона
- В `bootstrap5-eshop-main/assets/js/main.js` присутствуют демо-хендлеры Bootstrap Offcanvas для корзины (`#offcanvasCart`, `#cart-open`, `.closecart`), которых нет в продакшн-скрипте. Логика «наверх» и OwlCarousel — аналогична.
- Стили демо-шаблона почти совпадают, но без некоторых пользовательских добавлений (например, блоки поиска, часть sidebar-логики в продакшне расширены).

### Интеграция и порядок подключения
1. Подключите CSS: Bootstrap, Font Awesome, затем `public/assets/css/main.css`.
2. Подключите JS: jQuery → Bootstrap JS → OwlCarousel → Toastr → Livewire → `public/assets/js/main.js`.
3. Убедитесь, что в DOM присутствуют элементы из раздела «DOM-хуки»; иначе отключите соответствующие блоки кода или добавьте элементы.

### Советы по изменению/расширению
- При добавлении новых каруселей используйте класс `.owl-carousel-full` или переинициализируйте их на событие `livewire:navigated` аналогично.
- Для кастомизации Toastr измените опции в `main.js` и визуальные стили в `main.css` (блок Toastr).
- Если меняете структуру хедера, сохраните `#header-nav` или обновите логику скролла.
- Для поиска поддерживайте классы `.search-form`, `.search-results`, `.search-empty` или обновите соответствующие стили.
